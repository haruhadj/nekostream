node_modules
.next
.git
.gitignore
npm-debug.log
README.md
plan.md
Dockerfile
.dockerignore
docker-compose.yml

# Never bake secrets or a local database into the image
.env
.env.*
!.env.example
*.db
*.db-journal
*.db-wal
*.db-shm

# Source screenshots/pastes — not app assets, keep them out of the image
public/Pasted*
public/Snipaste*

# The Expo client is a second app against this server's API, never part of its
# image. Excluded as a whole directory because the bare `node_modules` rule
# above only matches the root one — without this, `COPY . .` would drag
# mobile/node_modules into the build context.
mobile
